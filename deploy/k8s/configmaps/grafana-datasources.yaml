# GENERATED by scripts/k8s-gen-configmaps.sh from: --from-file=deploy/grafana/provisioning/datasources
# Do not edit — change the source file(s) and re-run the script.
apiVersion: v1
data:
  prometheus.yml: |
    apiVersion: 1
    datasources:
      - name: Prometheus
        # Fixed uid so the provisioned dashboard JSON can reference it without a per-install lookup.
        uid: prometheus
        type: prometheus
        access: proxy
        url: http://prometheus:9090
        isDefault: true
        editable: false
kind: ConfigMap
metadata:
  name: grafana-datasources
