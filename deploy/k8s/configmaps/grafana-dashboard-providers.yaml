# GENERATED by scripts/k8s-gen-configmaps.sh from: --from-file=deploy/grafana/provisioning/dashboards
# Do not edit — change the source file(s) and re-run the script.
apiVersion: v1
data:
  skytrack.yml: |
    apiVersion: 1
    providers:
      - name: skytrack
        type: file
        # Re-read the JSON every 30s so edits to deploy/grafana/dashboards/*.json show up without restart.
        updateIntervalSeconds: 30
        allowUiUpdates: false
        options:
          path: /var/lib/grafana/dashboards
          foldersFromFilesStructure: false
kind: ConfigMap
metadata:
  name: grafana-dashboard-providers
